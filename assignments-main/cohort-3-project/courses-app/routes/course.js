const { Router } = require("express");
const courseRouter = Router();

courseRouter.post("/course/purchase", async (req, res) => {
    
})

courseRouter.get("/courses", async (req, res) => {
    
})

module.exports = {
    courseRouter,
}
